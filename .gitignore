node_modules/
**/node_modules/
contracts/out/
contracts/cache/
contracts/broadcast/
contracts/deployments/*.local.json
web/.next/
web/out/
.env
.env.local
*.log
.DS_Store
scripts/fork/.state/
scripts/fork/.snapshot.json
# Byte-identical duplicate of web/public/deployments/local.json; both are rewritten by every
# `make bootstrap`. The web one is served to the app, so that is the copy the repo keeps.
scripts/fork/deployments.local.json
scripts/node_modules/
__pycache__/
scripts/story/.story-state.json
scripts/story/state/

# Local anvil-fork deploy manifests (chain id 31337) are regenerated by every `make deploy`.
contracts/deployments/31337.json
