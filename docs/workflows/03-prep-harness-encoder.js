export const meta = {
  name: 'aqua-prep',
  description: 'Idea-independent prep: e2e Foundry harness against Aqua+custom SwapVM router, and a TS encoder cross-checked against Solidity',
  phases: [
    { title: 'Build', detail: 'harness + encoder in parallel' },
    { title: 'Verify', detail: 'independent verifier re-runs everything' },
  ],
}

const ROOT = '/Users/kirillrybkov/Desktop/project'
const SP = '/private/tmp/claude-501/-Users-kirillrybkov-Desktop-project/9e63dac7-1e5d-4fe3-9634-61767d65c76a/scratchpad'
const REFS = `${SP}/refs`

const COMMON = `
CONTEXT: Hackathon project (ETHGlobal ETHOnline 2026, 1inch 'Build an Aqua app' prize). Repo root: ${ROOT}. It is a git repo; DO NOT run git commit/push (the orchestrator commits). Contracts live in ${ROOT}/contracts (Foundry; deps in node_modules via npm: @1inch/aqua, @1inch/swap-vm, @1inch/solidity-utils, @openzeppelin/contracts, forge-std; remappings in remappings.txt; via_ir; solc 0.8.30). \`forge build\` already succeeds and compiles ${ROOT}/contracts/src/ProbeRouter.sol — a custom router = Simulator + SwapVM + AquaOpcodes with one custom opcode (ProbeScale at Opcode._d0). Reference clones of the upstream repos (with their full test suites) are at ${REFS}/swap-vm and ${REFS}/aqua — READ the upstream tests there to learn the exact APIs (e.g. ${REFS}/swap-vm/test/SwapVMAqua.t.sol, XYCSwapAqua.t.sol, AquaAccounting.t.sol, TakerTraits.t.sol, and the base test contracts they inherit). IMPORTANT API FACTS (current main branch, differs from README): SwapVM constructor is (aqua, weth, owner, name, version); Order = {maker, traits, data} where data = tokenA(20)‖tokenB(20)‖hook slices‖program, built via MakerTraitsLib.build(MakerTraitsLib.Args{...}) (tokenA<tokenB required); quote(order, amount, takerTraitsAndData) and swap(order, amount, takerTraitsAndData); taker data built via TakerTraitsLib.build(TakerTraitsLib.Args{...}) — includes isExactIn, isAToB, threshold, to, deadline, useTransferFromAndAquaPush, signature, instructionsArgs...; in Aqua mode hash(order)=keccak256(abi.encode(order)) which is the Aqua strategyHash and the maker calls aqua.ship(app=router, strategy=abi.encode(order), tokens, amounts) after approving Aqua; balances are loaded via AQUA.safeBalances(maker, router, orderHash, tokenIn, tokenOut); settlement via AQUA.pull/push. Instructions are libraries with .build(...) returning bytes and .opcode constants; programs are bytes.concat of builds. Work ONLY inside the directories you are assigned. Return a concise report of what you built, exact commands to run it, and the verified results (paste the actual test output summary).`

phase('Build')
const [harness, encoder] = await parallel([
  () => agent(`${COMMON}
YOUR DIRECTORIES: ${ROOT}/contracts/test, ${ROOT}/contracts/script, ${ROOT}/contracts/src/mocks (create), and you may add a Makefile/README notes in ${ROOT}/contracts. Do not modify ProbeRouter.sol or ProbeScale.sol except to fix a compile error.
TASK: Build a reusable end-to-end Foundry test harness for Aqua-backed SwapVM strategies executed through OUR custom router, and prove it works.
1) ${ROOT}/contracts/test/base/AquaSwapVMTestBase.sol: abstract base that (a) deploys a fresh Aqua (from @1inch/aqua/src/Aqua.sol — check its constructor) OR uses an existing address if an env var AQUA is set (for fork tests), (b) deploys a WETH mock (find one in ${REFS}/swap-vm/test/mocks/WETHMock.sol — copy into src/mocks or test/mocks) and 2-3 ERC20 mocks with 18 and 6 decimals, (c) deploys ProbeRouter(aqua, weth, owner), (d) helper functions: buildAquaOrder(maker, tokenA, tokenB, program) -> ISwapVM.Order using MakerTraitsLib with useAquaInsteadOfSignature=true; shipOrder(maker, order, tokens, amounts) that approves Aqua and calls aqua.ship(address(router), abi.encode(order), tokens, amounts) and returns strategyHash, asserting it equals router.hash(order); takerData(isExactIn, isAToB, threshold, to, useTransferFromAndAquaPush) builder; quote/swap wrappers; balance snapshot helpers reading aqua.rawBalances and ERC20 balances.
2) ${ROOT}/contracts/test/AquaXYC.t.sol: using the base, ship an XYCSwap program (bytes.concat(FeeFlatIn.build(...)?, XYCSwap.build()) — check exact builder signatures in ${REFS}/swap-vm/src/instructions/) with 10 WETH + 20,000 USDC(6 decimals), then as a taker execute exactIn A→B, exactOut A→B, exactIn B→A; assert token movements (maker wallet decreased/increased, taker wallet), Aqua virtual balances updated, quote()==swap() amounts, and the Swapped event. Also test the custom ProbeScale opcode in a program (prove custom opcode dispatch works via our router). Also test dock() returns virtual balances and subsequent swap reverts.
3) ${ROOT}/contracts/test/fork/AquaMainnetFork.t.sol: a fork test (skipped unless env FORK_RPC_URL set; use vm.envOr) that forks Ethereum mainnet via a public RPC (try https://ethereum-rpc.publicnode.com, https://eth.llamarpc.com, https://eth.drpc.org — pick one that works; run with \`FORK_RPC_URL=... forge test --match-path test/fork/*\`), uses the OFFICIAL Aqua at 0x1111113ccf1426a8e30e2bff5e005d929bf6a90a and real WETH 0xC02aaA39b223FE8D0A0e5C4F27eAD9083C756Cc2 / USDC 0xA0b86991c6218b36c1d19D4a2e9Eb0cE3606eB48, deploys ProbeRouter pointing at the official Aqua, deals tokens with deal(), ships, swaps, and asserts. This proves 'official Aqua contracts used' on a fork. Report whether the public RPC worked and how long the test took.
4) Run \`forge test -vv\` and make everything pass. Paste the summary. Note any surprises about the API (e.g. Aqua ship requirements like sorted tokens, tokensCount limits, approvals, safeBalances reverts) in a short ${ROOT}/contracts/NOTES.md.`, { label: 'harness', phase: 'Build' }),

  () => agent(`${COMMON}
YOUR DIRECTORIES: ${ROOT}/web/src/lib/swapvm (create) and ${ROOT}/contracts/test/encoding (create, for the Solidity cross-check harness only). The web app is Next.js 16 / TypeScript / viem 2.56 (installed in ${ROOT}/web). Use viem utilities (encodePacked, concatHex, keccak256, encodeAbiParameters, etc.).
TASK: Write a dependency-light TypeScript module that encodes SwapVM programs, Orders and taker traits EXACTLY as the Solidity libraries do, so the frontend can build orders, compute strategy hashes, ship to Aqua, and call quote/swap.
1) Read ${ROOT}/contracts/node_modules/@1inch/swap-vm/src/libs/{MakerTraits,TakerTraits,InstructionBuilder,InstructionArgs,MemoryPtr,OpcodeList}.sol and every instruction library in src/instructions/*.sol (their build() functions define the arg encodings — sizes of each field, e.g. push(uint256,size)). Also src/interfaces/ISwapVM.sol for the Order struct ABI.
2) Implement in ${ROOT}/web/src/lib/swapvm/: opcodes.ts (the Opcode enum values as a const object), instructions.ts (a builder function per instruction available in AquaOpcodes + the ones commonly composed: stop, revert, salt, jump, jumpIfDirection/TokenIn/TokenOut, deadline, onlyTakerTokenBalanceNonZero/Gte, onlyTakerTokenSupplyShareGte, onlyTxOriginTokenBalanceNonZero, xycSwap, xycConcentrateSwap (all variants), decay, feeFlatIn, feeFlatOut, feeProtocol, peggedSwap, extruction, staticBalances, dynamicBalances, limitSwap, limitSwapFullAmount, invalidateBit/TokenIn/TokenOut, dutchAuctionBalanceIn/Out, twapSwap, requireMinRate, adjustMinRate, oraclePriceAdjuster, baseFeeAdjuster, piecewiseLinearScaleBalanceIn/Out, validateSeriesEpoch, whitelist*, privateOrder — each producing Hex with the [opcode][len][args] header exactly like InstructionBuilder; plus a generic customInstruction(opcode, argsHex) for our own opcodes), order.ts (buildOrder(args) mirroring MakerTraitsLib.build incl. hook slices & indexes; orderHashAqua(order) = keccak256(abi.encode(order)) and the EIP-712 hash for signature mode; encodeStrategyForShip(order) = abi.encode(order)), taker.ts (buildTakerTraits(args) mirroring TakerTraitsLib.build incl. the slices header & flags), abi.ts (minimal ABIs for ISwapVM quote/swap/hash, IAqua ship/dock/rawBalances/safeBalances/events — extract from ${ROOT}/contracts/out/*.json artifacts or the upstream ABI file at ${REFS}/sdks/typescript/aqua/src/abi/Aqua.abi.ts), and index.ts. Export clean types. No external deps beyond viem.
3) CROSS-CHECK against Solidity: write ${ROOT}/contracts/test/encoding/EncodingVectors.t.sol which builds a set of instructions/programs/orders/takerTraits with the Solidity libraries and writes them as JSON (use vm.serializeBytes/vm.writeJson to ${ROOT}/contracts/test/encoding/vectors.json; add the path to fs_permissions in foundry.toml with access read-write) — then a vitest test in ${ROOT}/web/src/lib/swapvm/__tests__/encoding.test.ts that loads vectors.json and asserts the TS output matches every vector byte-for-byte (install vitest as a devDependency in ${ROOT}/web with npm; add a "test" script). Cover at least: every instruction builder with non-trivial args, an Aqua-mode order with and without hooks, orderHashAqua vs router.hash(order) (compute in Solidity via a deployed ProbeRouter), and taker traits with threshold+to+deadline+instructionsArgs+useTransferFromAndAquaPush both exactIn/exactOut and isAToB both ways. Run both sides and make them pass. Paste results.
4) Write ${ROOT}/web/src/lib/swapvm/README.md documenting usage in 30 lines.`, { label: 'ts-encoder', phase: 'Build' }),
])

phase('Verify')
const verify = await agent(`${COMMON}
You are an independent verifier. Two agents claim to have built (A) a Foundry e2e harness in ${ROOT}/contracts/test (+ fork test) and (B) a TS SwapVM encoder in ${ROOT}/web/src/lib/swapvm with a cross-check vitest suite. Their reports:
--- A ---
${harness}
--- B ---
${encoder}
--- end ---
Re-run everything yourself from scratch: \`cd ${ROOT}/contracts && forge build && forge test -vv\`; then the fork test with a working public RPC (FORK_RPC_URL=https://ethereum-rpc.publicnode.com or another); then \`cd ${ROOT}/web && npx vitest run\` (and \`npx tsc --noEmit\` to check types). Read the code critically: does the harness really use aqua.ship with app=router and abi.encode(order)? does the fork test really target 0x1111113ccf1426a8e30e2bff5e005d929bf6a90a? does the TS encoder cover the instruction set with correct field sizes (spot-check 5 builders against the Solidity source)? Fix any small issue you find directly (you may edit both areas), re-run, and report: pass/fail per suite with actual output, what you fixed, and any remaining caveats. Do not commit.`, { label: 'verify', phase: 'Verify', effort: 'xhigh' })

return { harness, encoder, verify }