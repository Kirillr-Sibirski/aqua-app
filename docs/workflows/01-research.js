export const meta = {
  name: 'aqua-research',
  description: 'Deep research: Aqua/SwapVM internals, hackathon rules, market & competitor landscape, UX benchmarks',
  phases: [
    { title: 'Research', detail: '14 parallel readers, each writes a KB file' },
    { title: 'Critique', detail: 'completeness critic finds gaps' },
    { title: 'Fill gaps', detail: 'one agent per gap' },
  ],
}

const SP = '/private/tmp/claude-501/-Users-kirillrybkov-Desktop-project/9e63dac7-1e5d-4fe3-9634-61767d65c76a/scratchpad'
const REFS = `${SP}/refs`
const KB = `${SP}/kb`

const COMMON = `
CONTEXT: We are building a hackathon submission for ETHGlobal ETHOnline 2026 (today is 2026-09-05), 1inch track, prize "Build an Aqua app": "Create a custom Aqua app that implements a sophisticated DeFi position. If you use SwapVM, you may modify SwapVM opcodes and define your own instructions. The final positions must be demonstrated through tests scripts or a UI. Projects that utilize SwapVM will be scored higher. Qualification: (1) Official Aqua/SwapVM contracts must be used (redeployments of a modified SwapVM contract is allowed); (2) Onchain execution of token transfers must be presented during the final demo (local forks are ok); (3) Proper git commit history."
Local clones of the official repos are at: ${REFS}/aqua (1inch/aqua), ${REFS}/swap-vm (1inch/swap-vm), ${REFS}/sdks/typescript/aqua (1inch/sdks TS aqua SDK). Aqua registry is deployed at 0x1111113ccf1426a8e30e2bff5e005d929bf6a90a and the SwapVM router at 0x111111338c5091e8440b67b168bae16a668ac0de on Ethereum, Base, Arbitrum, Optimism, Polygon, etc.
YOUR OUTPUT: Write a thorough, well-structured markdown knowledge-base file to the path given below (use Bash heredoc or the Write tool). It will be read by other engineers/agents who have NOT read the sources, so be concrete: include exact function signatures, struct layouts, code snippets, file:line references, numbers, and URLs. Prefer facts over opinions; mark anything uncertain as UNCERTAIN. Do not pad. Then return the structured summary.
`

const SCHEMA = {
  type: 'object',
  properties: {
    file: { type: 'string' },
    summary: { type: 'string', description: '5-10 sentence summary of the most decision-relevant findings' },
    key_facts: { type: 'array', items: { type: 'string' }, description: '10-25 crisp, verifiable facts' },
    open_questions: { type: 'array', items: { type: 'string' } },
    idea_seeds: { type: 'array', items: { type: 'string' }, description: 'product/position ideas that this research suggests (optional)' },
  },
  required: ['file', 'summary', 'key_facts', 'open_questions'],
}

const TASKS = [
  { key: 'aqua-core', prompt: `Read EVERY file under ${REFS}/aqua/src, ${REFS}/aqua/examples, ${REFS}/aqua/test/base, ${REFS}/aqua/test/AquaLifecycle.t.sol, AquaPushPull.t.sol, AquaBalances.t.sol, AquaShipDock.t.sol, AquaRouter.t.sol, ${REFS}/aqua/DEPLOY.md and the whitepaper PDF at ${REFS}/aqua/docs/whitepaper-aqua-1.0.pdf (use the Read tool with pages). Document: the exact IAqua interface (all functions, events, errors, struct Balance / uint248+uint8 packing), semantics and preconditions of ship/dock/pull/push/rawBalances/safeBalances (who may call what — e.g. pull is msg.sender=app, push requires approval, what happens on re-ship of same hash, dock of partially-consumed strategy, multi-token strategies, max tokens count), how strategyHash is computed and what "app" means (any contract address), AquaApp base contract (nonReentrantStrategy, _safeCheckAquaPush, transient storage), the XYCSwap example app end-to-end (strategy struct, swap flow, callback), AquaRouter purpose, the whitepaper's stated vision/terminology ("ships", "docks", etc.) and any hints about intended use cases and future direction. Include a minimal Foundry test recipe for: deploy mock ERC20s → approve Aqua → ship → swap through an app → assert balances/events. Note license constraints (Degensoft Aqua Source 1.1) relevant to a hackathon.`, file: `${KB}/aqua-core.md` },

  { key: 'swapvm-core', prompt: `Read ${REFS}/swap-vm/src/SwapVM.sol, src/libs/VM.sol, MemoryPtr.sol, MakerTraits.sol, TakerTraits.sol, OpcodeList.sol, InstructionBuilder.sol, InstructionArgs.sol, StorageSlots.sol, ProtocolFee.sol, src/interfaces/*.sol, src/routers/*.sol, src/opcodes/*.sol, and README.md + DEPLOY.md + TESTING.md. Document precisely: the Order struct and hashing (EIP-712 typehash, what fields), the full swap()/quote() execution flow step by step, the Context struct (VM, SwapQuery, SwapRegisters), runLoop semantics and nested runLoop, how instructions are dispatched (function pointer array, opcode index = array index), how a custom router is built (inherit SwapVM + Opcodes, override _instructions(); show a full compilable example that ADDS a new custom instruction to the opcode list, including the InstructionBuilder/ProgramBuilder usage from tests), MakerTraits bits and TakerTraits layout, hooks (IMakerHooks pre/post transfer) and taker callbacks (ITakerCallbacks), the WETH unwrap path, the transient reentrancy lock. MOST IMPORTANT — the Aqua mode: when useAquaInsteadOfSignature=true, exactly how SwapVM (a) authorizes the order (which Aqua balance is checked, what strategyHash equals — is it the orderHash? show code), (b) loads balances (from Aqua rawBalances/safeBalances?), (c) settles (pull to taker, push from taker; the useTransferFromAndAquaPush taker flag; isFirstTransferFromTaker), (d) which contract is the "app" in Aqua's mapping (the router address itself) — meaning that if WE deploy a modified router, makers ship() to the official Aqua with app=our router. Confirm this reading from code and cite lines. Also document AquaSwapVMRouter vs SwapVMRouter vs LimitSwapVMRouter differences (opcode sets), the Debug variants, and the deploy scripts (constructor args, deterministic deployment/CREATE3?).`, file: `${KB}/swapvm-core.md` },

  { key: 'swapvm-instructions', prompt: `Read EVERY file in ${REFS}/swap-vm/src/instructions/ (including interfaces/) and ${REFS}/swap-vm/src/strategies/Strategies.sol and src/libs/PeggedSwapMath.sol, Power.sol. For EACH instruction (Balances, LimitSwap, XYCSwap, XYCConcentrate, PeggedSwap, TWAPSwap, DutchAuction, Decay, FeeFlat, FeeProtocol, BaseFeeAdjuster, OraclePriceAdjuster, MinRate, PiecewiseLinearScale, SeriesEpochManager, Invalidators, Whitelist, TokenValidators, Controls, Jumps, Extruction, Debug) document: purpose, the exact function name(s) and 1D/2D/XD variants, argument encoding and the *ArgsBuilder helper signatures, what registers it reads/writes, whether it reads taker args (tryChopTakerArgs), storage it uses, ordering constraints and known interactions, and a one-line usage snippet. Pay special attention to: Extruction (how external logic is called — delegatecall or call? what interface must the target implement? what can it modify?), Jumps/Controls (conditional flow), TWAPSwap and DutchAuction (time-based pricing formulas), OraclePriceAdjuster (IPriceOracle interface), SeriesEpochManager (epoch/series invalidation — enables recurring orders?), PiecewiseLinearScale (what curve), XYCConcentrate (the concentrated liquidity math: sqrtPrice bounds, grow-liquidity variants, virtual reserves), PeggedSwap (curve params), Decay (Mooniswap virtual balances). Finish with a section 'What is NOT possible with existing opcodes' — capabilities that would require a custom instruction (e.g. reading external state, time-varying weights, multi-hop, cross-strategy accounting, conditional liquidity, etc.), and a section 'Strategies.sol' describing the prebuilt strategy builders.`, file: `${KB}/swapvm-instructions.md` },

  { key: 'swapvm-aqua-tests', prompt: `Read these tests in ${REFS}/swap-vm/test/: SwapVMAqua.t.sol, XYCSwapAqua.t.sol, AquaAccounting.t.sol, SwapVmAccounting.t.sol, ControlsAqua.t.sol, FeeAqua.t.sol, ProtocolFeeAqua.t.sol, TakerCallbackAquaNegative.t.sol, TransferModesCombinations.t.sol, NativePayment.t.sol, UnwrapWeth.t.sol, plus test/mocks/*.sol and any shared base/helper contracts they import (find them: grep for 'contract .*Test' and the imports; read the base test contract fully). Document: the exact test setup for an Aqua-backed SwapVM order (deploy Aqua? deploy which router? mint/approve, aqua.ship(app=router, strategy=?, tokens, amounts) — what bytes are passed as 'strategy' to ship() in Aqua mode: is it the abi-encoded Order? show code), how taker data is built for Aqua mode (empty signature, useTransferFromAndAquaPush, callbacks), how quote() is called via asView(), how balances are asserted after the swap, and the accounting rules (amountNetPulled, protocol fees in Aqua mode). Produce a COPY-PASTEABLE minimal Foundry test file (complete, compilable against the swap-vm repo layout) that: deploys Aqua + AquaSwapVMRouter, ships an XYC strategy with 2 tokens, executes exactIn and exactOut swaps as a taker, and checks Aqua balances. Then ALSO: run \`cd ${REFS}/swap-vm && ls node_modules/@1inch 2>/dev/null; forge build 2>&1 | tail -5\` (node deps may still be installing in the background from another process — if node_modules/@1inch/aqua is missing, wait with \`sleep 30\` up to 5 times and retry; if still missing run \`yarn install\` yourself) and report: whether it compiles with forge, how long it takes (use \`time\`), and whether \`forge test --match-contract SwapVMAqua\` passes and how long. Report exact compile errors if any.`, file: `${KB}/swapvm-aqua-tests.md` },

  { key: 'swapvm-custom-opcodes', prompt: `Study how to extend SwapVM with custom instructions and routers. Read ${REFS}/swap-vm/test/RunLoop.t.sol, Strategies.t.sol, MakerHooks.t.sol, OpcodeEnumCheck.t.sol, TWAPSwap.t.sol, DutchAuction.t.sol, SeriesEpochManager.t.sol, PiecewiseLinearScale.t.sol, Controls.t.sol, Debug.t.sol, test/mocks/BestRouteSelector.sol, MockMakerHooks.sol, test/invariants/*.sol, test/gas/*.sol, src/opcodes/*.sol, src/libs/OpcodeList.sol, src/libs/InstructionBuilder.sol, src/instructions/Extruction.sol, src/instructions/Debug.sol, and ${REFS}/swap-vm/.cursor/rules/*.mdc, TESTING.md. Document with COMPLETE code: (1) the anatomy of an instruction function (signature \`function _foo(Context memory ctx, bytes calldata args) internal\`; how args are parsed; how ctx.swap registers are mutated; how storage is accessed under isStaticContext; how to call ctx.runLoop() for wrap-around instructions), (2) how the opcode enum/list is defined and how the ProgramBuilder maps a function to an opcode index (p.build(fn, args)), and what must change to add a new opcode (OpcodeList? Opcodes contract? Debug variants?), (3) a complete worked example: a custom router 'MyRouter is SwapVM, AquaOpcodes' with ONE new instruction, a program that uses it, and a Foundry test that ships to Aqua and swaps — all compilable, (4) how BestRouteSelector/Extruction works as a way to run external logic (is it delegatecall — i.e. can an Extruction target read/write router storage? what are the safety implications), (5) how the invariants test base (CoreInvariants) is used, (6) gas numbers from snapshots for typical programs. Also list the opcode index table of AquaOpcodes (index → instruction) if derivable.`, file: `${KB}/swapvm-custom-opcodes.md` },

  { key: 'sdk-ts', prompt: `Read everything under ${REFS}/sdks/typescript/aqua (README, src/**, tests/**, package.json) and ${REFS}/sdks/typescript/README.md. Then run \`npm view @1inch/aqua\`, \`npm view @1inch/swap-vm\`, \`npm view @1inch/aqua-sdk\` and \`npm search 1inch swap-vm\` / \`npm view @1inch/swap-vm-sdk\` to discover any published TS packages for Aqua/SwapVM; if a package exists, download it into ${SP}/refs/npm/<name> via \`npm pack\` + tar and inspect what it ships (do they ship TS program builders / opcode encoders / ABIs?). Also check GitHub via WebFetch for https://github.com/1inch/sdks/tree/master/typescript (list of SDKs) and https://github.com/1inch/swap-vm (is there a TS/JS builder anywhere, e.g. in test/, scripts/, ignition/). Document: the TS Aqua SDK API (classes, methods, viem/ethers dependence, how to call ship/dock, how to read balances, how to parse events, how to compute strategyHash), and what we would need to write ourselves for a frontend to (a) encode a SwapVM program from instructions (bytecode format: [opcode][len][args]), (b) build MakerTraits/Order and TakerTraits bytes in TS (mirror the Solidity packing — give the exact bit layout from ${REFS}/swap-vm/src/libs/MakerTraits.sol and TakerTraits.sol), (c) call quote()/swap() via viem. Provide a TS skeleton for a minimal 'swapvm-ts' encoder module with the packing logic.`, file: `${KB}/sdk-ts.md` },

  { key: 'hackathon-rules', prompt: `Use WebFetch (and WebSearch if needed) on https://ethglobal.com/events/ethonline2026, https://ethglobal.com/events/ethonline2026/prizes, https://ethglobal.com/events/ethonline2026/prizes#1inch (try also https://ethglobal.com/events/ethonline2026/prizes/1inch), and any linked 1inch prize detail / resources pages (workshop videos, docs links, 'Aqua' resources) — fetch them. Document our 'bible': exact prize text for ALL 1inch prizes at this event (amounts, requirements, judging notes), total 1inch prize pool, event dates & submission deadline (with timezone — compute how many hours remain from 2026-09-05 22:00 local), submission requirements (project page fields, demo video length, README, live demo, open-source?), judging process (judges, criteria, how finalists are chosen), the event's general rules (team size, prior work policy, what counts as 'built during hackathon'), and any 1inch-provided resources (docs URLs, Discord, example repos, workshop recordings — fetch the workshop page/transcript if available and summarize what 1inch engineers emphasized). If the prize page JS-renders and WebFetch returns little, try WebSearch for 'ETHOnline 2026 1inch Aqua prize' and fetch cached/alternative sources. Be exhaustive and quote text verbatim where it matters.`, file: `${KB}/hackathon-rules.md` },

  { key: 'aqua-positioning', prompt: `Research how 1inch positions Aqua and SwapVM publicly. Use WebSearch + WebFetch: 1inch blog posts announcing Aqua (blog.1inch.io / 1inch.io/blog), the Aqua landing page (try https://aqua.1inch.io, https://1inch.io/aqua, https://app.1inch.io/aqua?), docs (https://docs.1inch.io — look for Aqua/SwapVM sections, fetch them), the Aqua whitepaper and SwapVM whitepaper (local PDFs at ${REFS}/aqua/docs/whitepaper-aqua-1.0.pdf and ${REFS}/swap-vm/docs/whitepaper-swap-vm-1.0.pdf — read them fully with the Read tool, pages param), X/Twitter threads by 1inch / Anton Bukov / Sergej Kunz about Aqua, podcast/interview summaries, Mirror/Medium posts, and any third-party analyses (The Block, Blockworks, DL News, Messari, Delphi) of Aqua. Document: the official narrative and vocabulary (self-custodial LP, 'ship', 'dock', 'shared liquidity layer', 'strategy', 'app', 'maker/taker/resolver'), the problems 1inch says Aqua solves, the target users they name (market makers? retail? protocols? DAOs?), how Aqua connects to the 1inch aggregator / Fusion / resolvers (does the 1inch router route through Aqua strategies? is there an Aqua tab in the 1inch app? which apps exist today?), any roadmap items, adoption metrics (TVL-equivalent 'shipped' liquidity, volume, number of makers) if published, and any explicitly mentioned example 'Aqua apps' 1inch wants to see. Also capture what 1inch engineers/judges seem to value (from talks/tweets): e.g. capital efficiency, composability, novel curves, MEV protection. End with a section: 'What would impress the 1inch team' (evidence-based).`, file: `${KB}/aqua-positioning.md` },

  { key: 'prior-winners', prompt: `Research prior hackathon projects built on 1inch Aqua / SwapVM and prior 1inch-track winners, to avoid duplication and calibrate the bar. Use WebSearch + WebFetch on ethglobal.com/showcase (search terms: 'Aqua', 'SwapVM', '1inch'), e.g. https://ethglobal.com/showcase?search=aqua, https://ethglobal.com/showcase?search=swapvm, https://ethglobal.com/showcase?search=1inch, and ETHGlobal 2026 events that had 1inch prizes (check https://ethglobal.com/events and the prize pages of 2026 events such as HackMoney/Prague/Cannes/New Delhi/Buenos Aires/Unite DeFi/ETHGlobal Open etc.). Also check Devfolio/Dorahacks/Encode hackathons with 1inch Aqua tracks, and GitHub search results for repos mentioning 'AquaApp' / 'useAquaInsteadOfSignature' / '1inch/aqua' (WebFetch https://github.com/search?q=%22AquaApp%22+language%3ASolidity&type=code may need auth — try https://github.com/1inch/aqua/network/dependents and https://github.com/1inch/swap-vm/network/dependents, and https://github.com/topics/aqua-app). Document for each found project: name, event, what it built, whether it used SwapVM/custom opcodes, whether it won, link. Then: summarize patterns of WINNING 1inch-track projects historically (2023-2026: Fusion+ cross-chain extensions, limit-order strategies, etc.) — what judges rewarded (novelty? completeness? UI? tests?). End with 'Ideas already taken' and 'Whitespace' lists.`, file: `${KB}/prior-winners.md` },

  { key: 'market-lp-pain', prompt: `Market research (WebSearch + WebFetch, cite sources with URLs and dates; prefer 2025-2026 sources): the current pain points of liquidity provision and onchain market making. Cover: impermanent loss / LVR magnitude (academic + industry numbers; e.g. 'LVR', Milionis et al., Uniswap LP profitability studies, 'most LPs lose money' data), capital fragmentation across pools/chains/venues, custody & smart-contract risk of depositing into pools/vaults (2025-2026 hack/loss totals, notable vault/pool exploits), idle capital problem for DAO treasuries and token issuers (how much stablecoin/treasury capital sits idle; DAO treasury sizes), MEV/sandwich losses to LPs and traders, rebalancing/gas costs of active management, the rise of vault managers and 'ALMs' (automated liquidity managers) and their problems, the professional MM landscape (Wintermute, GSR, Flow Traders...) and how they provide onchain liquidity (RFQ, PMMs), token issuers' need for liquidity (market-maker loan deals, their cost & controversies), stablecoin issuers & RWA issuers needing peg liquidity, and any 2026-specific narratives (e.g. intents, solver networks, AI agents trading onchain, 'agentic finance', perp DEX dominance, restaking, tokenized stocks/RWAs, prediction markets). Produce a RANKED list of pain points with (a) who suffers, (b) evidence/magnitude, (c) why existing solutions fall short, (d) whether a self-custodial, programmable liquidity primitive (Aqua+SwapVM) could address it and how. Be quantitative.`, file: `${KB}/market-lp-pain.md` },

  { key: 'market-competitors', prompt: `Competitive landscape research (WebSearch + WebFetch, cite URLs; prefer 2025-2026 info). Map every product category adjacent to 'self-custodial programmable liquidity': (1) AMMs & hooks: Uniswap v4 + notable hooks (Bunni v2, Arrakis/HOT, Angstrom/Sorella, EulerSwap, Doppler, Flaunch), Balancer v3, Curve, Maverick v2, Ambient/CrocSwap, Fluid DEX (Instadapp — smart collateral/debt as liquidity), Aerodrome/Velodrome; (2) intent/RFQ/PMM systems: 1inch Fusion/Fusion+, UniswapX, CoW Swap/CoW AMM, Bebop, Hashflow, Native, 0x RFQ; (3) ALMs/vaults: Gamma, Steer, Charm Alpha, Arrakis, ICHI, Aera, Morpho vaults, Yearn; (4) onchain limit/DCA/grid/TWAP tooling: 1inch Limit Orders, Mean Finance/Balmy, Orbs dTWAP/dLIMIT, Hummingbot, Gains/GMX order types; (5) self-custodial trading/treasury tools: Definitive, Karpatkey, Enzyme, Nested, Safe apps; (6) options/structured products onchain: Panoptic, Smilee, Thetanuts, Ribbon/Aevo, Rysk, Premia, Lyra/Derive, IVX; (7) 'liquidity as a service' & PoL: Berachain PoL, Olympus, Tokemak/Auto, Arrakis Pro, market-maker-loan alternatives; (8) EulerSwap-style 'lending-collateral-as-AMM' and similar capital-efficiency designs (Fluid, Ajna?). For each: what it does, custody model (funds locked? where?), programmability, who uses it, traction (TVL/volume 2026 if available), key weakness. Then produce a positioning matrix: axes 'custody (locked vs self-custodial)' × 'strategy programmability' × 'liquidity shared across strategies' and identify the whitespace Aqua+SwapVM uniquely occupies. End with 'Product angles that would be impossible/very hard without Aqua' — concrete, at least 10.`, file: `${KB}/market-competitors.md` },

  { key: 'position-catalog', prompt: `Research (WebSearch/WebFetch + your own quantitative reasoning) a catalog of 'sophisticated DeFi positions' that could be implemented as an Aqua app / SwapVM program, since the prize asks for exactly that. For each candidate give: name; what payoff/behavior it produces; who wants it and why (pain point); the math (formulas) at the level needed to implement in Solidity with 256-bit fixed point; which EXISTING SwapVM instructions could implement it (see instruction list: staticBalances, dynamicBalances, limitSwap, xycSwap, xycConcentrate (CLMM ranges), peggedSwap (stableswap), twapSwap, dutchAuction (in/out), decay, flatFee in/out, protocolFee, baseFeeAdjuster, oraclePriceAdjuster, requireMinRate/adjustMinRate, piecewiseLinearScale, seriesEpochManager, invalidators (bit/tokenIn/tokenOut), whitelist, tokenValidators, onlyTakerTokenBalance*/SupplyShare gates, deadline, salt, jump/jumpIf, extruction (external call)) and which would need a NEW custom instruction; complexity (hours for a strong team overnight); demo-ability (can it be shown on a local fork with visible token transfers?); and 'wow' factor. Candidates to evaluate at minimum: (a) replicating market makers / payoff replication via CFMM (Angeris-Evans-Chitra 'Replicating Market Makers', 'RMM-01'): covered call / put-writing positions expressed as AMM curves; (b) grid trading bot as a set of range orders; (c) TWAMM/DCA accumulation with price bands; (d) auto-rebalancing weighted portfolio (Balancer-style weights that change on schedule = 'liquidity bootstrapping pool' / TokenSets-like); (e) volatility-responsive dynamic fees / LVR-aware AMM (am-AMM, dynamic fee via oracle/realized-vol, 'toxic flow' pricing); (f) oracle-anchored concentrated liquidity that re-centers around an oracle price ('self-recentering CLMM'); (g) stop-loss / take-profit / trailing stop as maker strategies; (h) 'Dutch-auction liquidation' or treasury diversification sale (DAO sells token over time to buy stables, with price floor); (i) peg-defense / stablecoin issuer liquidity that only quotes near peg with widening spreads; (j) perpetual/funding-style positions; (k) conditional liquidity that only activates when a taker holds X (gated liquidity for allowlisted solvers); (l) multi-strategy 'liquidity router' where one wallet shares balance across several curves simultaneously (Aqua's core feature) — e.g. a 'liquidity OS' where an LP's WETH/USDC backs a CLMM + limit ladder + peg-arb at once; (m) basis/carry trades, delta-neutral LP hedged with perps (can Aqua interplay with lending protocols via hooks/extruction — e.g. 'JIT borrow to fill'?); (n) 'JIT liquidity from lending positions' (EulerSwap/Fluid style: quote from collateral that stays productive — Aqua's non-custody makes this natural: tokens in wallet can be in Aave aTokens? can an Aqua strategy quote aTokens/wstETH and the app unwrap? investigate); (o) agent-operated market making (an AI/off-chain agent re-ships strategies based on signals — cheap because no token transfers). Produce a scoring table (1-5) across: sophistication, Aqua-necessity (impossible without Aqua), SwapVM usage (custom opcode natural?), market pain, demo wow, feasibility overnight. Recommend top 5 with rationale and describe the custom opcode(s) each would need.`, file: `${KB}/position-catalog.md` },

  { key: 'ux-benchmarks', prompt: `Research best-in-class DeFi product UX to set the bar for a 'production-ready, million-users-feel' UI. Use WebSearch/WebFetch on design write-ups, and describe from knowledge: Uniswap (v4 app, position creation flow, range selector, fee tier UX), Aave v3/v4 app, Morpho, Hyperliquid, Jupiter, Pendle, Ethena, Aerodrome, Definitive.fi, Arrakis, Bunni, 1inch dApp itself (its visual language: colors, typography, layout — fetch https://app.1inch.io or design references; 1inch brand guidelines if public), Rabby/Zerion portfolio UX, Dune/DefiLlama data density patterns. Extract concrete patterns: (1) information architecture for an LP/position product (positions list → position detail → builder/wizard → simulate → confirm → activity), (2) position builder UX patterns (range sliders on price charts, preview of payoff curve, projected APR, risk badges, presets), (3) transaction UX (stepper: approve → ship, pending states, toasts, explorer links, error recovery), (4) data viz for liquidity curves/payoff diagrams/PnL, (5) trust signals (audits, TVL, docs, status), (6) visual design conventions in top DeFi apps 2026 (dark themes, typography e.g. Inter/Geist/Space Grotesk, accent colors, glassmorphism vs flat, density), (7) an explicit list of 'vibe-coded UI tells' to AVOID (default shadcn look, purple gradients, emoji icons, lorem-ipsum-ish copy, inconsistent spacing, fake data that looks fake, missing empty/loading/error states, no keyboard focus states, etc.), and (8) what makes a hackathon demo video/UI compelling to judges in 3 minutes. Include a proposed component inventory for our app and recommended stack (Next.js/Vite + React, wagmi/viem, RainbowKit/ConnectKit/Privy, Tailwind + which component approach, charting lib e.g. lightweight-charts/visx/recharts/d3, motion lib) with reasons and version pins that exist as of 2026 (verify via \`npm view <pkg> version\`).`, file: `${KB}/ux-benchmarks.md` },

  { key: 'fork-stack', prompt: `Establish the concrete local-fork demo stack and verify the official deployments. Using Bash with cast/anvil (installed) and public RPCs: (1) For Ethereum mainnet, Base, Arbitrum, Optimism, Polygon — run \`cast code 0x1111113ccf1426a8e30e2bff5e005d929bf6a90a --rpc-url <rpc> | head -c 100\` and same for 0x111111338c5091e8440b67b168bae16a668ac0de to confirm bytecode presence (try several public RPCs per chain: https://eth.llamarpc.com, https://ethereum-rpc.publicnode.com, https://cloudflare-eth.com, https://mainnet.base.org, https://base-rpc.publicnode.com, https://arb1.arbitrum.io/rpc, https://mainnet.optimism.io, https://polygon-rpc.com, etc.). (2) Probe on-chain activity: use \`cast logs\` over a recent block range (e.g. last ~50k blocks; find the Shipped event signature from ${REFS}/aqua/src/interfaces/IAqua.sol or ${REFS}/sdks/typescript/aqua/src/abi/Aqua.abi.ts — compute topic with \`cast sig-event\`/\`cast keccak\`) on Ethereum and Base to see how many ship() events exist and which app addresses are used (is the official SwapVM router the main app? are there other apps?). Also try the explorer APIs via WebFetch (etherscan/basescan address pages for both contracts — are they verified? transaction counts?). (3) Write the fork recipe: exact \`anvil --fork-url ... --fork-block-number ... --chain-id\` command, how to fund a demo wallet (anvil_setBalance, impersonation via anvil_impersonateAccount + transfer from a whale — identify 2-3 whale addresses for WETH and USDC on the chosen chain via explorer token holder pages or known addresses e.g. Binance/Coinbase hot wallets, aTokens contracts, Uniswap v3 pools), how to deploy our custom router to the fork with forge script pointing at the OFFICIAL Aqua address, how a browser wallet (MetaMask/Rabby) connects to the local anvil chain and how wagmi config should define the chain (chainId 31337 vs forked chain id — recommend which), plus pitfalls (fork RPC rate limits, --no-rate-limit, block time, --auto-impersonate, mining mode, state snapshots anvil_snapshot/revert for demo resets, anvil --dump-state/--load-state for reproducible demos). (4) Recommend which chain to fork (mainnet vs Base) for the demo considering token availability, RPC reliability, and where Aqua has activity. (5) Also note Chainlink price feed addresses for ETH/USD, BTC/USD on that chain (verify with cast call latestRoundData) — useful for oracle-based strategies — and Aave v3 Pool/aWETH/aUSDC addresses on that chain (verify with cast code). Write everything to the KB file.`, file: `${KB}/fork-stack.md` },
]

phase('Research')
log(`Launching ${TASKS.length} research agents`)
const results = await parallel(TASKS.map(t => () =>
  agent(`${COMMON}\nWRITE YOUR KB FILE TO: ${t.file}\n\nTASK (${t.key}):\n${t.prompt}`, { label: `research:${t.key}`, phase: 'Research', schema: SCHEMA })
    .then(r => ({ key: t.key, ...r }))
))
const ok = results.filter(Boolean)
log(`${ok.length}/${TASKS.length} research agents returned`)

phase('Critique')
const CRITIC_SCHEMA = {
  type: 'object',
  properties: {
    gaps: { type: 'array', items: { type: 'object', properties: { title: { type: 'string' }, why_it_matters: { type: 'string' }, task: { type: 'string' }, file: { type: 'string' } }, required: ['title', 'why_it_matters', 'task', 'file'] } },
    contradictions: { type: 'array', items: { type: 'string' } },
    overall: { type: 'string' },
  },
  required: ['gaps', 'contradictions', 'overall'],
}
const critic = await agent(`${COMMON}
You are the completeness critic. Read ALL files in ${KB}/ (ls it; read each fully). The next step is a design workshop where agents will propose and adversarially evaluate product ideas, and then a 10-hour build. Identify: (a) GAPS — decision-relevant facts that are missing, unverified, or contradictory (e.g. 'how does SwapVM Aqua mode compute the strategyHash — not confirmed from code', 'no verified whale addresses', 'hackathon deadline unknown', 'no data on whether the official router has an Extruction opcode', 'unclear if custom router must be an AquaApp'), each with a concrete self-contained research task and the KB file to write; (b) CONTRADICTIONS between files, resolved if you can by reading the source under ${REFS}; (c) an overall assessment. Limit gaps to the 6-10 that matter most for choosing and building a winning Aqua/SwapVM app. Return the structured result; also append a '## Critic notes' section to ${KB}/_critic.md.`, { label: 'critic', phase: 'Critique', schema: CRITIC_SCHEMA, effort: 'xhigh' })

phase('Fill gaps')
const gaps = critic ? critic.gaps.slice(0, 10) : []
log(`Filling ${gaps.length} gaps`)
const fills = await parallel(gaps.map(g => () =>
  agent(`${COMMON}\nWRITE YOUR KB FILE TO: ${g.file} (append if it exists; add a clearly titled section)\n\nGAP TO FILL: ${g.title}\nWHY IT MATTERS: ${g.why_it_matters}\nTASK: ${g.task}\nRead relevant existing KB files in ${KB} first so you do not duplicate; verify against source code under ${REFS} and/or the web; be exact.`, { label: `gap:${g.title.slice(0, 40)}`, phase: 'Fill gaps', schema: SCHEMA })
    .then(r => ({ gap: g.title, ...r }))
))

return { research: ok.map(r => ({ key: r.key, summary: r.summary, key_facts: r.key_facts, open_questions: r.open_questions, idea_seeds: r.idea_seeds || [] })), critic: critic ? { overall: critic.overall, contradictions: critic.contradictions, gaps: gaps.map(g => g.title) } : null, fills: fills.filter(Boolean).map(f => ({ gap: f.gap, summary: f.summary, key_facts: f.key_facts })) }