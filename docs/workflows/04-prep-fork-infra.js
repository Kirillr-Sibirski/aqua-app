export const meta = {
  name: 'aqua-prep2',
  description: 'Idea-independent infra: Base-fork bootstrap scripts + oracle mock, web wallet/contract plumbing, and a fork test filling LIVE maker liquidity through the official router',
  phases: [
    { title: 'Build', detail: 'fork scripts / web plumbing / live-fill proof in parallel' },
    { title: 'Verify', detail: 'independent verifier re-runs everything' },
  ],
}

const ROOT = '/Users/kirillrybkov/Desktop/project'
const SP = '/private/tmp/claude-501/-Users-kirillrybkov-Desktop-project/9e63dac7-1e5d-4fe3-9634-61767d65c76a/scratchpad'
const REFS = `${SP}/refs`
const KB = `${SP}/kb`

const COMMON = `
CONTEXT: Hackathon project (ETHGlobal ETHOnline 2026, 1inch 'Build an Aqua app' prize; deadline Sep 13). Repo root: ${ROOT} (git; DO NOT commit/push — the orchestrator does). Layout: ${ROOT}/contracts (Foundry; official @1inch/aqua + @1inch/swap-vm in node_modules; our custom router src/ProbeRouter.sol = Simulator+SwapVM+AquaOpcodes+1 custom opcode; 69 passing tests incl. test/fork/AquaMainnetFork.t.sol against the OFFICIAL Aqua 0x1111113CCf1426A8E30e2bfF5E005d929bF6a90a; test/base/AquaSwapVMTestBase.sol has ship/quote/swap helpers; read contracts/NOTES.md) and ${ROOT}/web (Next.js 16 / React 19 / Tailwind 4 / wagmi 3.7 / viem 2.56; ${ROOT}/web/src/lib/swapvm is a verified TS encoder for SwapVM programs/orders/taker traits + ABIs — read its README.md and index.ts).
KNOWLEDGE BASE (read what you need): ${KB}/fork-stack.md (verified fork recipe, RPCs, whale addresses, Chainlink/Aave addresses, wagmi notes; recommendation: fork BASE chain 8453 at a pinned block, anvil chain id 31337, Tenderly public gateway https://gateway.tenderly.co/public/base, fallback https://mainnet.base.org / https://base.drpc.org), ${KB}/oracle-mocking-fork.md (how to move Chainlink prices on the fork via anvil_setCode/anvil_setStorageAt), ${KB}/swapvm-core.md, ${KB}/aqua-core.md, ${KB}/_critic.md (§1: deployed official router is v1.0.2 'World A' with 5-arg quote/swap and different taker-traits encoding vs the HEAD 'World B' we build on; 159 of 436 live Base strategies are ungated; KycNFT gate details). Upstream clones: ${REFS}/swap-vm (HEAD), ${REFS}/swap-vm-v1.0.2 (deployed tag, if present — else \`git clone --depth 1 --branch v1.0.2 https://github.com/1inch/swap-vm ${REFS}/swap-vm-v1.0.2\`), ${REFS}/aqua, ${REFS}/sdks. Work ONLY inside your assigned directories. Everything you deliver must be actually run and verified; paste real output in your report. Return a concise report: files, commands, verified results, caveats.`

phase('Build')
const [fork, web, live] = await parallel([
  () => agent(`${COMMON}
YOUR DIRECTORIES: ${ROOT}/scripts (create; a small standalone npm package with viem + tsx, package.json name @aqua-app/scripts) and ${ROOT}/contracts/script (you may add scripts, not modify existing ones except DeployProbeRouter.s.sol if needed). Also you may add root-level ${ROOT}/Makefile targets and ${ROOT}/.env.example.
TASK: Build the local-fork demo infrastructure and verify it end to end.
1) ${ROOT}/scripts/fork/start.sh: starts anvil forking Base (8453) at a PINNED block (pick a recent block; record it in the script) with chain id 31337, --auto-impersonate, sensible --block-time (or automine) and --no-rate-limit; supports ANVIL_FORK_URL env override; prints the RPC URL; optionally --state ${ROOT}/scripts/fork/.state for reuse. Test that it starts and \`cast block-number\` works.
2) ${ROOT}/scripts/fork/bootstrap.ts (run via tsx): against the running fork, (a) deploys our router from the Foundry artifact (${ROOT}/contracts/out/ProbeRouter.sol/ProbeRouter.json — read bytecode+abi; constructor (aqua=0x1111113CCf1426A8E30e2bfF5E005d929bF6a90a, weth=Base WETH 0x4200000000000000000000000000000000000006, owner=deployer)) using anvil account #0 — make the contract name/artifact path configurable via env ROUTER_ARTIFACT so the real router can be swapped in later; (b) funds demo wallets (anvil accounts #0-#3 and an optional DEMO_ADDRESS env — an EOA the user will connect via MetaMask) with ETH, WETH, USDC, cbBTC on Base using whale impersonation (whales per kb/fork-stack.md; verify balances) or anvil_setStorageAt if impersonation fails; (c) writes ${ROOT}/web/public/deployments/local.json AND ${ROOT}/scripts/fork/deployments.local.json with {chainId, rpcUrl, blockNumber, aqua, officialRouter, router, weth, usdc, cbBtc, chainlink:{ethUsd,btcUsd,usdcUsd}, aave:{pool}, accounts:[...]} — the web app will read this. (d) prints a summary table.
3) ${ROOT}/scripts/fork/oracle.ts: exports setOraclePrice(feedAddress, priceUsd) implementing the verified technique from kb/oracle-mocking-fork.md (anvil_setCode of a mock aggregator at the proxy address + anvil_setStorageAt for the price/updatedAt, keeping decimals()=8 and latestRoundData() shape) and a CLI: \`tsx oracle.ts eth 3100\` sets Base ETH/USD to $3100. Verify with cast call latestRoundData before/after and that updatedAt is fresh (block timestamp) so staleness checks pass. Also add \`tsx time.ts +3600\` (evm_increaseTime + evm_mine) and \`tsx snapshot.ts save|restore\` (anvil_snapshot/anvil_revert) helpers.
4) A top-level ${ROOT}/Makefile with targets: fork, bootstrap, oracle (ARGS), test, test-fork, web. And ${ROOT}/scripts/README.md (short).
5) VERIFY the whole chain: start fork → bootstrap → check router.AQUA() == official Aqua via cast → ship a strategy from anvil account #1 through our router using the TS encoder in ${ROOT}/web/src/lib/swapvm (import it relatively or copy the compiled logic — do NOT modify it) — i.e. approve Aqua, aqua.ship(router, encodeStrategyForShip(order), [WETH,USDC], amounts), then quote and swap 0.1 WETH from account #2 with useTransferFromAndAquaPush, and print balances/events. Put that as ${ROOT}/scripts/fork/smoke.ts and make it pass. Paste the real output.`, { label: 'fork-infra', phase: 'Build' }),

  () => agent(`${COMMON}
YOUR DIRECTORIES: ${ROOT}/web/src/lib/chain (create), ${ROOT}/web/src/lib/contracts (create), ${ROOT}/web/src/hooks (create), ${ROOT}/web/src/components/wallet (create), ${ROOT}/web/src/app/providers.tsx (create), ${ROOT}/web/src/app/dev/** (create), and minimal edits to ${ROOT}/web/src/app/layout.tsx to wrap children in the providers (do not restyle anything else; the visual design will be done later by other agents). You may add npm deps to ${ROOT}/web (do not upgrade existing ones). Do NOT touch ${ROOT}/web/src/lib/swapvm.
TASK: Wallet + contract plumbing for the app, against the local anvil fork (chain id 31337, http://127.0.0.1:8545) with Base (8453) as the second chain.
1) lib/chain/: define the local fork chain for viem/wagmi (id 31337, name 'Aqua Fork (Base)', native ETH, rpc http://127.0.0.1:8545, multicall3 address as on Base 0xcA11bde05977b3631167028862bE2a173976CA11 which exists on the fork since it's a Base fork) and export a wagmi config (wagmi v3 — READ node_modules/wagmi and node_modules/@wagmi/core README/type definitions to use the CURRENT v3 API correctly; do not assume v2) with injected connector(s) (MetaMask/Rabby) and, if wagmi v3 offers it, a mock/'burner' connector using anvil account #1 private key for a no-wallet demo mode (env NEXT_PUBLIC_DEMO_PRIVATE_KEY, default anvil #1). SSR-safe setup (cookie storage or 'use client' boundaries as Next 16 requires).
2) lib/contracts/: load addresses from /deployments/local.json (fetched at runtime from public/, with a typed schema and a fallback to env NEXT_PUBLIC_* vars); export typed contract handles using the ABIs in ${ROOT}/web/src/lib/swapvm/abi.ts (swapVmAbi, aquaAbi, erc20Abi) + a Chainlink AggregatorV3 ABI.
3) hooks/: useDeployments(), useTokenBalances(address, tokens[]) (multicall), useAquaBalances(maker, app, strategyHash, tokens[]) (aqua.rawBalances multicall), useShippedStrategies(maker) — reads Aqua 'Shipped' events from the fork via viem getLogs from the deployment block (events have NO indexed params — filter client-side by maker and app==our router), decodes the strategy bytes with decodeOrder() from lib/swapvm to recover tokens + program, and returns strategies with live rawBalances + a docked flag; useQuote(order, amount, takerTraitsHex) (eth_call to router.quote via simulateContract/readContract), useShip() and useDock() write hooks (approve if needed → aqua.ship / aqua.dock) with tx state; useSwap() (router.swap with useTransferFromAndAquaPush, approve router if needed); useOraclePrice(feed).
4) app/dev/page.tsx: a plain, unstyled-but-tidy diagnostics page (this is NOT the product UI): connect wallet / demo mode toggle, show chain + deployments, token balances, list of my shipped strategies with virtual balances, a form to ship an XYC WETH/USDC strategy (uses buildAquaOrder + xycSwap + salt from lib/swapvm), a quote+swap form, and a dock button. Every action shows tx hash + receipt status.
5) VERIFY: you need a running fork with deployments — another agent is building ${ROOT}/scripts/fork/{start.sh,bootstrap.ts} concurrently; if ${ROOT}/web/public/deployments/local.json does not exist yet, create your own temporary fork (anvil --fork-url https://gateway.tenderly.co/public/base --chain-id 31337 --port 8545, if 8545 is busy use 8546 and NEXT_PUBLIC_RPC_URL) and deploy ProbeRouter yourself with forge create (constructor: 0x1111113CCf1426A8E30e2bfF5E005d929bF6a90a, 0x4200000000000000000000000000000000000006, deployer) to test against; write local.json in the same schema {chainId, rpcUrl, blockNumber, aqua, officialRouter:0x111111338c5091E8440b67B168bAe16a668AC0De, router, weth:0x4200000000000000000000000000000000000006, usdc:0x833589fCD6eDb6E08f4c7C32D4f71b54bdA02913, cbBtc:0xcbB7C0000aB88B473b1f5aFd9ef808440eed33Bf, chainlink:{ethUsd:0x71041dddad3595F9CEd3DcCFBe3D1F4b0a16Bb70,...}, accounts:[...]}. Run \`npm run build\` (must pass typecheck+lint), \`npm test\` (existing vitest must still pass), then run \`next dev\` and exercise the /dev page's read paths with a node/tsx script or curl of any API route you add — at minimum prove useShippedStrategies logic works by a vitest/tsx test that ships a strategy via viem against the fork and then decodes it back through the same decode path. Paste real output.`, { label: 'web-plumbing', phase: 'Build' }),

  () => agent(`${COMMON}
YOUR DIRECTORIES: ${ROOT}/contracts/test/fork/live (create) and ${ROOT}/contracts/test/fork/v102 (create, for vendored v1.0.2 encoding helpers). Do not modify other files except adding to ${ROOT}/contracts/NOTES.md and, if strictly needed, ${ROOT}/contracts/foundry.toml (append only).
TASK: Prove qualification rule #1 in the strongest possible way — a Foundry fork test that fills REAL, LIVE maker liquidity on Base through the OFFICIAL, UNMODIFIED SwapVM router 0x111111338c5091E8440b67B168bAe16a668AC0De against the OFFICIAL Aqua registry, and then ships our own strategy to OUR router against the same registry in the same test (one wallet, two apps, one registry).
Steps: (1) Read kb/_critic.md §1 and kb/fork-stack.md §2/§10 and kb/swapvm-core.md §14 (World A vs World B API deltas). The deployed router is v1.0.2: quote/swap are 5-arg (order, tokenIn, tokenOut, amount, takerTraitsAndData) with selectors 0x44aa5f14/0xf4d2d412; taker traits encoding differs from HEAD; Aqua-mode orders need empty signature. Clone the tag if not present: git clone --depth 1 --branch v1.0.2 https://github.com/1inch/swap-vm ${REFS}/swap-vm-v1.0.2 and read its src/libs/TakerTraits.sol, MakerTraits.sol, interfaces/ISwapVM.sol and test/SwapVMAqua.t.sol. (2) Discover live strategies: on a Base fork (vm.createSelectFork with https://gateway.tenderly.co/public/base or fallback RPCs; pin a block), find recent Aqua 'Shipped' events whose app == official router (use \`cast logs\` offline first to pick 3 candidate (maker, strategyHash, strategy bytes) with WETH/USDC and NO KycNFT gate (no opcode 0x21 with target 0x26FFc7D378E8e49Be2c483295A3e3E511F96a468 in the program; decode the v1.0.2 order data) and with non-zero aqua.safeBalances at the pinned block; hardcode those in the test as constants with the block number, since Foundry cannot query logs. The scratchpad file ${SP}/work/base_full3.raw.shipped.json (if it exists) already contains 436 decoded Shipped payloads — use it. (3) Test A: taker = anvil-style EOA funded via deal(); approve official router; build v1.0.2 taker traits (vendor the minimal encoding into test/fork/v102/TakerTraitsV102.sol, mirroring the tag's TakerTraitsLib.build) with useTransferFromAndAquaPush; quote() then swap() 0.05 WETH → USDC against the live strategy; assert Swapped/Pulled/Pushed events, maker wallet USDC/WETH deltas, taker deltas, Aqua virtual balance deltas. Also assert quote == swap. (4) Test B: same test contract: deploy OUR ProbeRouter(officialAqua, WETH, owner); as a new maker, ship a WETH/USDC XYC strategy to the official Aqua with app = our router; swap through our router; then show rawBalances for (maker, ourRouter) and prove the official router cannot pull from it and vice-versa (safeBalances revert for the wrong app). (5) Gated-strategy bonus (optional, only if quick): find a RES/KycNFT holder via logs and vm.prank(holder, holder) so tx.origin passes the gate. (6) Run with FORK_RPC_URL set; make the tests skip cleanly when unset. Paste the real output, the exact strategy hashes/makers used, and gas.`, { label: 'live-fill', phase: 'Build' }),
])

phase('Verify')
const verify = await agent(`${COMMON}
You are an independent verifier. Three agents report:
--- FORK INFRA ---
${fork}
--- WEB PLUMBING ---
${web}
--- LIVE FILL ---
${live}
--- end ---
Re-run everything from scratch: kill any stray anvil (pkill -f anvil), then \`make fork\` (or scripts/fork/start.sh) in the background, \`make bootstrap\`, \`tsx scripts/fork/smoke.ts\`, \`tsx scripts/fork/oracle.ts eth 3100\` + cast call check; then \`cd contracts && forge test\` and the fork suites with FORK_RPC_URL; then \`cd web && npm run build && npm test\`. Read the code critically: does bootstrap really deploy against the official Aqua address? does the live-fill test really call the official router 0x111111338c… on a real strategy? is the wagmi v3 API used correctly (check against node_modules types)? does /dev page compile and use the hooks? Fix small issues directly (you may edit all three areas), re-run, and report pass/fail per item with actual output, what you fixed, and remaining caveats. Leave the fork running? No — stop it at the end (pkill -f anvil). Do not commit.`, { label: 'verify', phase: 'Verify', effort: 'xhigh' })

return { fork, web, live, verify }